// objects refrence type
// arrays are goog but not suffucent
// for real world data
// objects store key value pairs
// objects don't have index

// how create objects
const person = {
    name: "Abdul RAhman",
    age: 22,
    hobbies: ["coding", "learning", "sleeping"]

};
console.log(person);

// how to access data from objects
console.log(person.name);
console.log(person.hobbies);

// how to add key value pairs to objects 
person.gender = 'male';
console.log(person);